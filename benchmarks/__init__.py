"""Benchmarking package for the M5 harness.

Modules are invoked via ``python -m benchmarks.<name>`` from the repo root, e.g.::

    python -m benchmarks.bench_attention
    python -m benchmarks.plot
    python -m benchmarks.naive_memory
"""
